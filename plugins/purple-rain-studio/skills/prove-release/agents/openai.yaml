interface:
  display_name: "Release Proof"
  short_description: "Prove a product is ready before calling it done"
  brand_color: "#6B3FA0"
  default_prompt: "Use $prove-release to test, deploy, and prove this release."
policy:
  allow_implicit_invocation: true
