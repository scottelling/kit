interface:
  display_name: "Smooth Elevation"
  short_description: "Give floating surfaces one clean edge"
  default_prompt: "Use $apply-smooth-elevation to apply the correct shadow depth and integrated edge to every elevated surface."
  brand_color: "#6B3FA0"
policy:
  allow_implicit_invocation: true
